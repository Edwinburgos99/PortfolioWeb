---
interface props{
    job: Job
}

const {date, title, description}= Astro.props.job
---
<ul class="w-full max-w-2xl space-y-8 text-left">
    <li class="ms-4 border-l-2 border-gray-900 dark:border-gray-900 pl-6 relative">
        <div class="absolute -left-2 top-2 w-4 h-4 bg-gray-900 dark:bg-blue-300 rounded-full border-2 border-white dark:border-gray-900 shadow-md"></div>
        <time class="text-xl font-semibold text-gray-900 dark:text-gray-900 leading-tight">{date}</time>
        <h3 class="text-xl font-semibold text-gray-900 dark:text-gray-900 leading-tight">{title}</h3>
        <p class="mt-2 text-xl text-gray-900 dark:text-gray-900 leading-relaxed">{description}</p>
    </li>
</ul>